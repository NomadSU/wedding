services:
  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped

  api:
    build: ./api
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASS=197288zz
      - DB_PATH=/data/rsvp.db
    volumes:
      - ./data:/data
    restart: unless-stopped
